<div class="center row  user-card">
    <div class="col-12">
        <div class="card-top">
            {{!--
            <h1>User Counter</h1> --}}
            <i class="fas fa-user-circle"></i>
        </div>
        <div class="card-bottom">
            {{#each this.greetedUsers}} Username : {{this.name}}
            <hr>
            <br>Greet Count : {{this.count}} </li>
            <hr> {{/each}}
        </div>
        <a href="/">
            <button>Home</button>
        </a>
    </div>
</div>